# Kubernetes development secret creation command
kubectl create secret generic jwt-secret --from-literal=JWT_KEY=<your-secret>

# Kubernetes expose load balancer command
kubectl expose deployment ingress-nginx-controller --target-port=80 --type=LoadBalancer -n kube-system
